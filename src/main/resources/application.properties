# --- Server ---
server.port=8080

# --- App Info ---
spring.application.name=CakeShop

# --- DataSource (PostgreSQL local) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/cakeshop
spring.datasource.username=postgres
spring.datasource.password=410042
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA/Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Logging ---
logging.level.root=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework.web=INFO

# --- JWT ---
jwt.secret=p6upep/y+VdvGrzplGbF06PmPvvTFp4Hl9xPmSrEvqk=
jwt.expiration=86400000
